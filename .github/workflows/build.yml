name: Build

on:
  push:
    branches:
      - development

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar g++
        run: |
          sudo apt-get update
          sudo apt-get install -y g++

      - name: Compilar
  	run: |
    	  g++ cpp/Calc.cpp cpp/test_Calc.cpp -o programa